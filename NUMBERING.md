# Mycelium 编号法则

一共四层,各管一件事,互不混用:

| 层 | 格式 | 管什么 | 例 |
|---|---|---|---|
| 孢子号 | `MYC-<族><5位>` | 生物身份(这只"是谁") | `MYC-TRY6HP` |
| 实体号 | `#NNN` / `U<年月>-NNN` | 物理个体(这一台) | `#001` / `U2608-003` |
| 部件号 | `<系列><编号>[-<件>] vX.Y.Z` | 工程零件 | `MC01-F v1.5.4` |
| 变体号 | `C-XX` 十六进制 | 方块的面组合 | `C-33` |

---

## 1 · 孢子号(生物身份)

**`MYC-` + 族字母 + 5 位 Crockford Base32**,共 10 字符。

- **从句子的种子哈希(xmur3)直接派生**——引擎生成形体用的同一个数。
  同一句话 → 同一只生物 → 同一个号,不需要登记表,网页端和实体端天然一致。
- 族字母:`T`ender `C`alm cu`R`ious `D`reamy com`P`anion `L`onely
- Base32 用 Crockford 字符表(无 0/O/1/I,不会念错、不怕手写)
- 5 位 = 3300 万组合,32 位哈希取低 25 位

```
"轻轻地生长" → xmur3 → 0x6E3D**** → MYC-TRY6HP
```

刻哪里:kit 图纸封面(已实现)、句子卡、网页孢子页 URL。

## 2 · 实体号(物理个体)

同一句话可以做出多台,所以生物身份和物理个体分开编:

- **对外(卡片/刻字)**:`MYC-TRY6HP · #002` —— 第 2 台这只生物。
  限量款直接用全局序号:`No.007 / 20`。
- **对内(质检/售后)**:`U<YYMM>-<序>`,如 `U2608-003` = 2026 年 8 月第 3 台。
  贴在核心舱内侧(不可见处),关联:出厂日期、固件版本、磁铁批次、测试记录。

一句话规则:**浪漫的号给外面,追溯的号藏里面。**

## 3 · 部件号(工程零件)

`<系列><编号>-<件代号> v<主>.<次>.<修>`

| 代号 | 件 |
|---|---|
| `MC01-F/L` | 核心舱框架/盖板(v1.5.4,已被 MC02 取代) |
| `MC02-P` | 核心背板 84×84×3(现 v0.2) |
| `MC02-AB` | 合板载板 55×60(A+B 合一) |
| `U/G/T` | 充电口块 / 喇叭网孔块 / 排线通道块 |
| `EP9-1…9` | 眼部拼件(3×3,按 kit 图行序:1=左上 … 9=右下) |
| `JIG-P` | 极性治具 |
| `GA-M` | 磁袋校准件 |

版本语义:主版本=接口变了(装不上旧件);次版本=形状优化(仍兼容);修订=只改打印参数。
**STL 文件名带版本,老版本永不覆盖**(仓库里已是这个习惯:`_v3`、`v154`)。

## 4 · 变体号(方块的面组合)

六个面按 **右 左 前 后 上 下** 各占一个 bit(bit5→bit0),拼成两位十六进制:

```
C-3F = 111111 六面全耦合(内部块)
C-33 = 110011 右左+上下耦合,前后全平(薄板块,最常见)
C-08 = 001000 只有前面耦合(尾梢块)
C-00 = 000000 六面全平(孤块,理论上不该出现)
```

- 好念、可刻(方块底面可以压一个 2mm 小字)、40 种变体不会混
- 位序永远固定,任何人拿号能反推面组合
- 功能块前缀替换:`W-`窗块 `H-`孔块(后接同样的十六进制)

## 5 · 一台完整机器的"身份证"示例

```
孢子号   MYC-TRY6HP          ← 句子决定,刻在句子卡
实体号   #001 · U2608-001    ← 这一台,内侧贴纸
核心舱   MC01-F v1.5.4 + MC01-L v1.5.4
固件     m0-eyes @ 46f816f
外壳     284 cubes · 40 variants (C-33 ×63 …) · EP9 ×1
```

## 6 · 已落实到代码的部分

- `scripts/spore3d.mts`:生成 `sporeId`,写进 meta JSON,构建日志打印
- `hardware/kit_sheet.py`:图纸封面标题和信息栏印孢子号
- `hardware/kit_cubes.py`:变体 STL 与清单改用 `C-XX` 编号

尚未做(需要时说一声):方块底面压印变体号浮雕;句子卡生成器;`U` 号台账表格。
