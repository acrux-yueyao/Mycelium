/**
 * TreeHoleInput — the anonymous sentence portal.
 * STEP 2 STUB. Step 4 will implement textarea + submission flow.
 */
export interface TreeHoleInputProps {
  onSubmit: (text: string) => void;
  disabled?: boolean;
}

export function TreeHoleInput(_props: TreeHoleInputProps) {
  return null;
}
