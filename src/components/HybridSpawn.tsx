/**
 * HybridSpawn — rainbow hybrid birth with expanding aura ring + scale pop.
 * STEP 2 STUB. Step 8 will implement framer-motion animation.
 */
export interface HybridSpawnProps {
  id: string;
  x: number;
  y: number;
  parentIds: [string, string];
}

export function HybridSpawn(_props: HybridSpawnProps) {
  return null;
}
